import React from "react";

const Sidebar: React.FC = () => {
    return (
        <div className="w-64 bg-gray-200 p-4 flex flex-col">
            <h2 className="text-lg font-bold mb-4">Financial Chatbot</h2>
            {/* Add sidebar content like navigation, user profile, etc. */}
        </div>
    );
};

export default Sidebar;